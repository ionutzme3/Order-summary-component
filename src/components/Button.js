import React from "react";

import classes from "./Button.module.css";

const Button = () => {
    return (
<       button className={classes["order-summary"]} >Proceed to Payment</button>
    );
};

export default Button;